# Mock-Typscript-React-FrontEnd
Interacts with the Backend And fetches the User-List, Uses Redux for State Management, Post and Add User Details in the Backend

# steps to run
1. Run command  "npm i" when you are in the current directory of the repo.
2. To seed the initial data as per the requirement for the UI, run "npm run start"

